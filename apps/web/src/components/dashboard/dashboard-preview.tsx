import { Activity, ArrowUpRight, GitCommitHorizontal } from "lucide-react";
import { Badge, Card } from "@trace/ui";
import { workspaceFixture } from "@/mocks/fixtures/workspace";
export function DashboardPreview() { return <><section className="metric-grid" aria-label="Illustrative workspace metrics">{workspaceFixture.metrics.map((metric, index) => <Card className="metric-card" key={metric.label}><div><span>{metric.label}</span>{index === 0 && <Activity size={18} aria-hidden="true"/>}</div><strong>{metric.value}</strong><small>{metric.note}</small></Card>)}</section><section className="dashboard-grid"><Card className="activity-card"><header className="section-heading"><div><span className="eyebrow">Latest signals</span><h2>Recent development activity</h2></div><Badge>Fixture data</Badge></header><div className="activity-list">{workspaceFixture.activity.map(item => <article key={`${item.repository}-${item.time}`}><span className="activity-icon"><GitCommitHorizontal size={18}/></span><div><strong>{item.message}</strong><p>{item.contributor} in <span>{item.repository}</span></p></div><time>{item.time}</time></article>)}</div></Card><Card className="focus-card"><span className="eyebrow">Day 1 focus</span><h2>Frontend foundation</h2><p>The application shell, page routes, mock boundary, and accessible design system are ready for feature work.</p><ul><li><span/>Independent of backend services</li><li><span/>Responsive navigation</li><li><span/>Typed UI primitives</li></ul><div className="next-marker">Next: authentication UI <ArrowUpRight size={17}/></div></Card></section></>; }
