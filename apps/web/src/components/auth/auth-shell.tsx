import type { ReactNode } from "react";
import Link from "next/link";
import { Radio } from "lucide-react";
export function AuthShell({ title, description, note, children }: { title: string; description: string; note: string; children?: ReactNode }) { return <main className="auth-page"><section className="auth-panel"><Link className="brand auth-brand" href="/"><span className="brand-mark"><Radio size={18}/></span><span>Trace</span></Link><div className="auth-copy"><span className="eyebrow">Secure developer workspace</span><h1>{title}</h1><p>{description}</p></div>{children}<p className="planned-note">{note}</p></section><aside className="auth-aside" aria-label="Product overview"><div><span className="signal-line"/><h2>Turn development activity into a clear daily narrative.</h2><p>Trace connects repository work, contributors, and reports without replacing your account identity.</p></div><dl><div><dt>Identity</dt><dd>Trace account first</dd></div><div><dt>Integration</dt><dd>GitHub connects later</dd></div><div><dt>Reports</dt><dd>Structured and editable</dd></div></dl></aside></main>; }
